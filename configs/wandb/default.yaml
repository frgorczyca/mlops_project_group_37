# configs/wandb/default.yaml
use_wandb: False  # Set to True to enable Weights & Biases logging
save_artifact: False  # Set to True to save artifact in Weights & Biases registry
project_name: "text-detect"
run_name: "test-run"